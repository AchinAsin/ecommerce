name: ecommerce_app
description: "Created this app to learn flutter and dart. It is a simple ecommerce app with a home screen, product screen, cart screen and checkout screen."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
